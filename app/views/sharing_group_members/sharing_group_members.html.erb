<p id="notice"><%= notice %></p>

<h1 class="text-info">Teilen-Gruppe für <%= @resource.name %></h1>

<p>
  Mitglieder der Teilen-Gruppe können auf die Dinge zugreifen, die du mit ihnen teilst. 
</p>

<table class="table">
  <thead>
    <tr>
      
      <th>Email</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @sharing_group_members.each do |sharing_group_member| %>
      <tr>        
        <td><%= sharing_group_member.email %></td>
        <td><%= sharing_group_member.shareable.name %></td>
        <td><%= sharing_group_member.shareable_type %></td>
        <td>
          <%= link_to remove_sharing_group_member_list_path({:sharing_group_member_id => sharing_group_member.id}), method: :delete, format: :html, data: { confirm: 'Are you sure?' }, :class => "btn btn-outline-danger" do %>
            <%= fa_icon "trash" %> Löschen
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= form_with(model: [SharingGroupMember.new], url: add_sharing_group_member_list_path, local: true, method: :post) do |form| %>

    <div class="input-group mb-3">          
      <%= form.text_field(:email, :class => "form-control")  %>
      <div class="input-group-append">            
        <button class="btn btn-primary">
          <%= fa_icon "plus" %> Hinzufügen
        </button>            
      </div>
    </div>        

  <% end %>     


