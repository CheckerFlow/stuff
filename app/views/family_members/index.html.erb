<p id="notice"><%= notice %></p>

<h1 class="text-info">Mitglieder</h1>

<p>
  Mitglieder können auf alle Gebäude, Räume, Ablagen und Dinge zugreifen genau wie du.
</p>

<table class="table">
  <thead>
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @family_members.each do |family_member| %>
      <tr>
        <td><%= family_member.name %></td>
        <td><%= family_member.email %></td>
        <!--<td><%= link_to 'Anzeigen', family_member %></td>-->
        <td>
          <%= link_to edit_family_member_path(family_member), :class => "btn btn-outline-primary" do %>
            <%= fa_icon "pen" %> Ändern
          <% end %>
        </td>
        <td>
          <%= link_to family_member, method: :delete, data: { confirm: 'Are you sure?' }, :class => "btn btn-outline-danger" do %>
            <%= fa_icon "trash" %> Löschen
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to new_family_member_path, :class => "btn btn-primary" do %>
  <%= fa_icon "plus"%> Neues Mitglied
<% end %>
