<h2>Demo</h2>

<div class="row">
  <div class="col-sm-12 col-md-6 col-lg-4">

    <%= form_for(@user, as: @user, url: session_path(User), method: :post) do |f| %>          
        <%= f.hidden_field :email, :value => "demo@klarschiff.app" %>
        <%= f.hidden_field :password, :value => "Klarschiff" %>
      
        <div class="actions">
            <%= f.submit "Demo Login", :class => "btn btn-primary" %>
        </div>
    <% end %>

  </div>
</div>
<br/>

